# ============================================================
# Medical Virtual Assistant - Project Requirements
# ============================================================
# This project is a Node.js / React application.
# Use npm to install dependencies (NOT pip).
#
# HOW TO INSTALL:
#   Frontend (React + Vite):  cd client && npm install
#   Backend  (Express API):   cd server && npm install
#
# ============================================================

# ---- FRONTEND (React + Vite) — client/ folder ----
# Runtime dependencies:
react@^18.2.0
react-dom@^18.2.0
react-router-dom@^6.23.0
react-icons@^4.12.0

# Dev dependencies:
@vitejs/plugin-react@^4.2.0
vite@^4.5.0
tailwindcss@^3.3.2
autoprefixer@^10.4.14
postcss@^8.4.27

# ---- BACKEND (Node + Express) — server/ folder ----
# Runtime dependencies:
express@^4.18.2
cors@^2.8.5

# ---- SYSTEM / RUNTIME REQUIREMENTS ----
# Node.js  >= 18.x   (https://nodejs.org/)
# npm      >= 9.x    (bundled with Node.js)
